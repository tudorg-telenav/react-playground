import * as React from 'react';

interface SettingsProps {
};

export default class Header extends React.Component<SettingsProps, {}> {

    render() {
        return (
            <div>SET THE SETTINGS</div>
        );
    }
}